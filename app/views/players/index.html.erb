<center><h1>Roster</h1></center>

<center>
<button type="button" class="btn btn-default"><%= link_to 'Roster', players_path %></button>
<button type="button" class="btn btn-default"><%= link_to 'Dynasty Rankings', rankings_players_path %></button>
<button type="button" class="btn btn-default"><%= link_to 'Depth Chart', depthchart_players_path %></button>
</center>

<div class="row">
 <div class="row col-md-5">
    <table class="table">
      <thead>
        <tr>
          <th>Position</th>
          <th>Player</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>QB</td>
            <% @players.each do |player| %>     
              <% if player.position == "QB" && player.positional_ranking == 1 %>   
                <td><%= link_to player.player, edit_player_path(player) %></td>
          </tr>
          <% end %>
        <% end %>
        
        <tr>
            <td>RB</td>
            <% @players.each do |player| %>  
            <% if player.position == "RB" && player.positional_ranking == 1 %>   
                <td><%= link_to player.player, edit_player_path(player) %></td>
          </tr>
          <% end %>
        <% end %>

        <tr>
            <td>RB</td>
            <% @players.each do |player| %>  
            <% if player.position == "RB" && player.positional_ranking == 2 %>   
                <td><%= link_to player.player, edit_player_path(player) %></td>
          </tr>
          <% end %>
        <% end %>

        <tr>
            <td>WR</td>
            <% @players.each do |player| %>  
            <% if player.position == "WR" && player.positional_ranking == 1 %>   
                <td><%= link_to player.player, edit_player_path(player) %></td>
          </tr>
          <% end %>
        <% end %>

        <tr>
            <td>WR</td>
            <% @players.each do |player| %>  
            <% if player.position == "WR" && player.positional_ranking == 2 %>   
                <td><%= link_to player.player, edit_player_path(player) %></td>
          </tr>
          <% end %>
        <% end %>

        <tr>
            <td>TE</td>
            <% @players.each do |player| %>  
            <% if player.position == "TE" && player.positional_ranking == 1 %>   
                <td><%= link_to player.player, edit_player_path(player) %></td>
          </tr>
          <% end %>
        <% end %>

        <tr>
            <td>FLEX</td>
            <% @players.each do |player| %>  
            <% if (player.position == "RB" && (player.positional_ranking != 1 || player.positional_ranking != 2)) || (player.position == "WR" && (player.positional_ranking != 1 || player.positional_ranking != 2)) || (player.position == "TE" && player.positional_ranking != 1) %>   
                <td><%= link_to player.player, edit_player_path(player) %></td>
          </tr>
          <% end %>
        <% end %>

        <tr>
            <td>FLEX</td>
            <% @players.each do |player| %>  
            <% if player.position == "RB" && player.positional_ranking == 1 %>   
                <td><%= link_to player.player, edit_player_path(player) %></td>
          </tr>
          <% end %>
        <% end %>

        <tr>
            <td>D</td>
            <% @players.each do |player| %>  
            <% if player.position == "D" && player.positional_ranking == 1 %>   
                <td><%= link_to player.player, edit_player_path(player) %></td>
          </tr>
          <% end %>
        <% end %>

        <tr>
            <td>D</td>
            <% @players.each do |player| %>  
            <% if player.position == "D" && player.positional_ranking == 2 %>   
                <td><%= link_to player.player, edit_player_path(player) %></td>
          </tr>
          <% end %>
        <% end %>

        <tr>
            <td>D</td>
            <% @players.each do |player| %>  
            <% if player.position == "D" && player.positional_ranking == 3 %>   
                <td><%= link_to player.player, edit_player_path(player) %></td>
          </tr>
          <% end %>
        <% end %>

        <tr>
            <td>K</td>
            <% @players.each do |player| %>  
            <% if player.position == "K" && player.positional_ranking == 1 %>   
                <td><%= link_to player.player, edit_player_path(player) %></td>
          </tr>
          <% end %>
        <% end %>

      </tbody>
    </table>
</div>
</div>

<center><button type="button" class="btn btn-default"><%= link_to 'Add a New Player', new_player_path %></button></center>
<br>
